<?xml version="1.0" encoding="UTF-8" ?>
<!-- 级别从高到低：OFF 、 FATAL 、 ERROR 、 WARN 、 INFO 、 DEBUG 、 TRACE 、 ALL -->
<!-- 日志输出规则：根据当前ROOT级别，日志输出时，级别高于root默认的级别时会输出 -->
<!-- 以下每个配置的 filter 是过滤掉输出文件里面其它级别的日志信息，只记录指定级别的日志 -->
<!-- scan 当此属性设置为true时，配置文件如果发生改变，将会被重新加载，默认值为true。 -->
<!-- scanPeriod 设置监测配置文件是否有修改的时间间隔，如果没有给出时间单位，默认单位是毫秒。当scan为true时，此属性生效。默认的时间间隔为1分钟。 -->
<!-- debug 当此属性设置为true时，将打印出logback内部日志信息，实时查看logback运行状态。默认值为false。 -->
<configuration scan="true" scanPeriod="60 seconds" debug="false">
    <!-- 动态日志级别 -->
<!-- <jmxConfigurator/> -->

    <!-- 定义常量，类似与key-value的形式，log.home_dir作为日志文件的输出路径，之后用${log.home_dir}取出-->
    <property name="log.home_dir" value="./logs"/>
    <!-- 定义日志文件的输出文件名-->
    <property name="log.app_name" value="tsp"/>
    <!-- 定义常量log.maxHistory，日志最大的历史60天 -->
    <property name="log.maxHistory" value="60"/>
    <!-- 定义单个日志文件最大的大小-->
    <property name="log.maxSize" value="10MB"/>
    <!-- 定义日志文件保留的总的大小，0 表示不限制 -->
    <property name="log.totalSizeCap" value="5GB"/>

    <!-- ConsoleAppender 控制台输出日志 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level] [%thread] %logger - %msg%n</pattern>  <!-- 设置日志输出格式 -->
        </encoder>
    </appender>

    <!-- ERROR级别日志，滚动记录文件，先将日志记录到指定文件，当符合某个条件时，将日志拆分记录到其他文件 RollingFileAppender-->
    <appender name="ERROR_APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- 过滤器，只记录ERROR级别的日志 -->
        <!-- 过滤器会根据onMath 和 onMismatch接收或拒绝日志。 -->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <!-- 设置过滤级别 -->
            <level>ERROR</level>
            <!-- 用于配置符合过滤条件的操作,超过ERROR级别的日志输出到日志文件当中-->
            <onMatch>ACCEPT</onMatch>
            <!-- 用于配置不符合过滤条件的操作，没超过ERROR级别的日志进行拦截 -->
            <onMismatch>DENY</onMismatch>
        </filter>

        <!-- 最常用的滚动策略，它根据时间来制定滚动策略.既负责滚动也负责触发滚动， 即可以根据时间和文件大小进行拆分 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!--日志输出位置可相对、和绝对路径 -->
            <fileNamePattern>
                <!-- 下面中的%d表示安装天进行拆分日志文件，%i表示当某一天中日志文件大小超过MaxFileSize标签规定的大小，i的取值会从1，2，3.....n这样的进行命令差分后的文件-->
                ${log.home_dir}/error/${log.app_name}-error-%d{yyyy-MM-dd}-%i.gz
            </fileNamePattern>
            <!-- 可选节点，控制保留的归档文件的最大数量，超出数量就删除旧文件,假设设置每个月滚动，且<maxHistory>是60天， 则只保存最近60天的文件，删除之前的旧文件。注意，删除旧文件是，那些为了归档而创建的目录也会被删除 -->
            <maxHistory>${log.maxHistory}</maxHistory>
            <!--按照文件大小进行拆分-->
            <MaxFileSize>${log.maxSize}</MaxFileSize>
            <!-- 日志文件的总大小，0 表示不限制 -->
            <totalSizeCap>${log.totalSizeCap}</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>
                <!-- 设置日志输出格式 -->
                %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- INFO级别日志 appender -->
    <appender name="INFO_APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>INFO</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${log.home_dir}/info/${log.app_name}-info-%d{yyyy-MM-dd}-%i.gz</fileNamePattern>
            <maxHistory>${log.maxHistory}</maxHistory>
            <MaxFileSize>${log.maxSize}</MaxFileSize>
            <totalSizeCap>${log.totalSizeCap}</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level] %logger - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- DEBUG级别日志 appender -->
    <appender name="DEBUG_APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>DEBUG</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${log.home_dir}/debug/${log.app_name}-debug-%d{yyyy-MM-dd}-%i.gz</fileNamePattern>
            <maxHistory>${log.maxHistory}</maxHistory>
            <MaxFileSize>${log.maxSize}</MaxFileSize>
            <totalSizeCap>${log.totalSizeCap}</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level] %logger - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ALL级别日志 appender，所有级别的日志都会输出-->
    <appender name="ALL_APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${log.home_dir}/all/${log.app_name}-all-%d{yyyy-MM-dd}-%i.gz</fileNamePattern>
            <maxHistory>${log.maxHistory}</maxHistory>
            <MaxFileSize>${log.maxSize}</MaxFileSize>
            <totalSizeCap>${log.totalSizeCap}</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level] %logger - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 异步写入日志，提升性能 -->
    <appender name="ASYNC_ERROR_APPENDER" class="ch.qos.logback.classic.AsyncAppender">
        <!-- 不丢失日志。默认的，如果队列的 80% 已满,则会丢弃 TRACT、DEBUG、INFO 级别的日志 -->
        <discardingThreshold>0</discardingThreshold>
        <!-- 更改默认的队列的深度，该值会影响性能。默认值为 256 -->
        <queueSize>256</queueSize>
        <appender-ref ref="ERROR_APPENDER"/>
    </appender>
    <appender name="ASYNC_INFO_APPENDER" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold>0</discardingThreshold>
        <queueSize>512</queueSize>
        <appender-ref ref="INFO_APPENDER"/>
    </appender>
    <appender name="ASYNC_DEBUG_APPENDER" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold>0</discardingThreshold>
        <queueSize>1024</queueSize>
        <appender-ref ref="DEBUG_APPENDER"/>
    </appender>
    <appender name="ASYNC_ALL_APPENDER" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold>0</discardingThreshold>
        <queueSize>1024</queueSize>
        <appender-ref ref="ALL_APPENDER"/>
    </appender>





    <!-- name:用来指定受此loger约束的某一个包或者具体的某一个类。 level:用来设置打印级别（日志级别），大小写无关：TRACE, DEBUG, INFO, WARN, ERROR, ALL 和 OFF，还有一个特殊值INHERITED或者同义词NULL，代表强制执行上级的级别。如果未设置此属性，那么当前loger将会继承上级的级别。 addtivity:是否向上级loger传递打印信息。默认是true。 -->
    <!--org.springframework.web包下的类的日志输出-->
    <logger name="org.springframework.web" additivity="false" level="INFO">
        <appender-ref ref="DEBUG_APPENDER"/>
        <appender-ref ref="ALL_APPENDER"/>
    </logger>
    <logger name="org.springframework.scheduling.quartz.LocalDataSourceJobStore" level="info"/>
    <logger name="org.springframework.security.web" level="info"/>
    <logger name="org.quartz.core.QuartzSchedulerThread" level="info"/>
    <logger name="org.apache.http.impl.conn.PoolingHttpClientConnectionManager" level="info"/>
    <logger name="com.modern.common.config.HttpTraceLogFilter" level="info"/>
    <logger name="io.lettuce.core" level="info"/>

    <logger name="org.springframework.data.redis" level="info"/>





    <!-- 开发环境 -->
    <springProfile name="dev">
        <root level="DEBUG">
            <appender-ref ref="CONSOLE"/>
            <!-- <appender-ref ref="ASYNC_ERROR_APPENDER"/> -->  <!-- 注释掉本行则不生成该日志文件 -->
            <!-- <appender-ref ref="ASYNC_INFO_APPENDER"/> -->
            <!-- <appender-ref ref="ASYNC_DEBUG_APPENDER"/> -->
            <!-- <appender-ref ref="ASYNC_ALL_APPENDER"/> -->
        </root>
    </springProfile>

    <!-- 测试环境 -->
    <springProfile name="test">
        <root level="DEBUG">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_ERROR_APPENDER"/>
            <appender-ref ref="ASYNC_INFO_APPENDER"/>
            <appender-ref ref="ASYNC_DEBUG_APPENDER"/>
            <appender-ref ref="ASYNC_ALL_APPENDER"/>
        </root>
    </springProfile>

    <!-- 生产环境 -->
    <springProfile name="prod">
        <root level="DEBUG">
            <!-- <appender-ref ref="CONSOLE"/> -->
            <appender-ref ref="ASYNC_ERROR_APPENDER"/>
            <appender-ref ref="ASYNC_INFO_APPENDER"/>
            <!-- <appender-ref ref="ASYNC_DEBUG_APPENDER"/> -->
            <!-- <appender-ref ref="ASYNC_ALL_APPENDER"/> -->
        </root>
    </springProfile>

</configuration>