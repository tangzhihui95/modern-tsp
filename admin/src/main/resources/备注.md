2024.08.13增加新表：
```sql
create table tsp_vehicle_sys_role
(
    id          bigint auto_increment comment '主键'
        primary key,
    vehicle_id      bigint        null comment 'tsp_vehicle表id',
    role_id     bigint        null comment 'sys_role表role_id',
    update_by   varchar(55)   null comment '更新者',
    update_time datetime      null comment '更新时间',
    create_by   varchar(55)   null comment '创建者',
    create_time datetime      null comment '创建时间',
    is_delete   int default 0 null comment '是否删除 1-是、0-否'

) ENGINE = InnoDB CHARACTER SET = utf8mb4 COLLATE = utf8mb4_general_ci COMMENT = '角色车辆关联表' ROW_FORMAT = Dynamic;


delete tsp_car_role_relation
```

2024.8.14
1.前台新增定时任务：系统监控->定时任务->新增
任务名称：TspInformationTask.informationStatusChange
任务分组：默认
调用方法：TspInformationTask.informationStatusChange
cron表达式：0 0/50 * * * ?
错误策略：放弃执行
是否并发：禁止
状态：正常

2.修改所有定时任务 错误策略为：放弃执行（即错过执行的定时任务，错过就错过）
任务组名均改为：默认

2024.08.22
问题：java.lang.NoClassDefFoundError: ch/qos/logback/classic/spi/ThrowableProxy
解决：原因为This error happens when spring applications jar are rebuilt, removed, or updated during execution.
解决方法为先stop容器，再start容器，而不能直接restart容器。
docker stop tsp_8081
docker start tsp_8081
docker stop nginx
docker start nginx

2024.09.11
给tsp_vehicle_license表添加索引：
```sql
ALTER TABLE `tsp_vehicle_license` ADD KEY `actable_idx_idx_tsp_vehicle_id` (`tsp_vehicle_id`)
```

2024.12.29
增加2个数据库表：tsp_statistics_vehicle_daily，tsp_statistics_vehicle_all_daily。（见gen_table.sql）
增加1个定时任务：
前台新增定时任务：系统监控->定时任务->新增
任务名称：每日统计车辆数据
任务分组：默认
调用方法：VehicleDailyStatisticsTask.vehicleDailyStatistics
cron表达式：0 10 0 * * ? *
错误策略：放弃执行
是否并发：禁止
状态：正常


2024.01.08
设置npm源为淘宝新镜像源
npm config set registry https://registry.npmmirror.com






# 列出所有docker容器
docker ps -a

# 获取docker容器的重启次数
docker inspect --format='{{.RestartCount}}' 0058889d6403
```